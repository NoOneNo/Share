<resources>

    <!-- Base application theme. -->
    <style name="ShareAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:homeAsUpIndicator">@mipmap/btn_back</item>-->
    </style>
    <style name="ShareAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="ShareAppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="ShareAppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
