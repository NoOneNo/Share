<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hengye.share" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ShareAppTheme"
        android:name=".BaseApplication">
        <activity
            android:name="com.hengye.share.ui.activity.MainActivity"
            android:launchMode="singleTop"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hengye.share.ui.activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName="com.hengye.share.ui.activity.MainActivity"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hengye.share.ui.activity.MainActivity" />
        </activity>

        <activity
            android:name="com.hengye.share.ui.activity.TopicGalleryActivity"
            android:parentActivityName="com.hengye.share.ui.activity.MainActivity"
            android:theme="@style/FullScreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hengye.share.ui.activity.MainActivity" />
        </activity>

        <activity
            android:name=".ui.activity.FragmentActivity">
        </activity>

        <activity
            android:name=".ui.activity.setting.SettingActivity"
            android:label="@string/title_activity_setting">
        </activity>

        <activity
            android:name="com.hengye.share.ui.activity.TestActivity">
        </activity>
    </application>

</manifest>
